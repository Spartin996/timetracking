<?php

// Database Connect
// Optional manual setup: copy this file up one directory as config.php and update the values.
// Prefer opening the app and using install.php, which writes config.php for you.
// You can also change the timezone here if needed.

$dbhost = "localhost";
$dbuser = "root";
$dbpass = "root";
$db = "time_sheets";

// Change the line below to your timezone!
date_default_timezone_set('Australia/Brisbane');
